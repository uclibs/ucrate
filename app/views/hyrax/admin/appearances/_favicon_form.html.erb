<%= simple_form_for @form, url: admin_appearance_path do |f| %>
  <div class="card-body">
    <% require_image = @form.favicon? ? false : true %>
    <%= f.input :favicon, as: :file, wrapper: :vertical_file_input, required: require_image, hint: t('hyrax.admin.appearances.show.forms.favicon.hint'), hint_html: { class: 'border-left-accent' } %>
    <%= image_tag @form.favicon.url, class: "img-fluid", alt: "Favicon preview" if @form.favicon? %>
  </div>
  <div class="card-footer">
    <%= f.submit class: 'btn btn-primary float-right' %>
  </div>
<% end %>
<% if @form.favicon? %>
  <div class="card-footer">
    <%= simple_form_for @form.site, url: main_app.site_path(@form.site) do |f| %>
      <%= f.submit 'Remove favicon', class: 'btn btn-danger', name: :remove_favicon %>
    <% end %>
  </div>
<% end %>
